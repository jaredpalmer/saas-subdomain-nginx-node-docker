version: "2"
services:
    web:
      build: "./web"
      ports:
        - "3000:3000"
    nginx:
      build: "./nginx"
      ports:
        - "80:80"
      hostname:
        - "twork.io:45.55.251.114"
      links:
        - web:web
